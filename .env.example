# Never commit the real file. On Render these are dashboard env vars, not a file.
TELEGRAM_TOKEN=          # BotFather
OWNER_CHAT_ID=           # your numeric Telegram id — the allowlist is one value
GROQ_API_KEY=            # console.groq.com, free tier
TURSO_URL=               # libsql://tala-<you>.turso.io
TURSO_TOKEN=             # turso db tokens create tala
